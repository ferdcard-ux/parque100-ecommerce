<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema base DayNight: en Android 15 (targetSdk 35) el sistema impone
         edge-to-edge, por eso las barras son transparentes y los insets
         se aplican en MainActivity con ViewCompat. Los colores de texto y
         fondos vienen de values/colors.xml y values-night/colors.xml. -->
    <style name="Theme.Parque100" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorSecondary">@color/accent</item>
        <item name="android:colorBackground">@color/background</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:forceDarkAllowed">false</item>
    </style>
</resources>
